
  # Student Registration Form UI

  This is a code bundle for Student Registration Form UI. The original project is available at https://www.figma.com/design/8BjkcpAdXHmS4pS46MUrWc/Student-Registration-Form-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  